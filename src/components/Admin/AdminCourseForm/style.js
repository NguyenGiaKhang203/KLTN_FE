import styled from 'styled-components';

export const ModalWrapper = styled.div`
  .ant-form-item-label > label {
    font-weight: 500;
  }

  .ant-input,
  .ant-input-number,
  .ant-select-selector {
    border-radius: 8px;
  }

  .ant-modal-body {
    padding-top: 16px;
  }
`;
